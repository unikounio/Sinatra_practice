<div class="header">
    <div class="header_name">メモアプリ</div>
    <form class="add" method="get" action="/new">
        <input type="submit" value="追加">
    </form>
</div>

<ul>
    <% @memos.each do |memo| %>
    <li><a href="/show/<%= @memos.index(memo) %>"><%= memo["title"] %></a></li>
    <% end %>
</ul>
